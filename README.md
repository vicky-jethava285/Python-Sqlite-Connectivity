# Python-Sqlite-Connectivity
This project demonstrates how to connect Python with an SQLite database using the built-in sqlite3 module. It covers database creation, table creation, data insertion, fetching records, and basic operations.  This is useful for beginners to understand how databases work with Python without installing any external software.
